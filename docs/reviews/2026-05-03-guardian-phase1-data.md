# Guardian — Phase 1 data pipeline

**VERDICT:** APPROVE

## Причины

- Структура Chroma/Qdrant коллекций не менялась (эта сессия только SQLite + Markdown).
- Обратная совместимость карточек SillyTavern не затронута.
- Нет секретов в репозитории; зависимость PDF — опциональная (`requirements-data.txt`).
- DI и `docker-compose` host-mode не затронуты.

## Замечания (не блокирующие)

- При конфликте строк с одинаковым `zip_code` в CSV сохраняется последняя запись (`INSERT OR REPLACE`); для японских данных это обычно приемлемо.
- Автоизвлечённый Markdown из PDF нужно пересматривать перед загрузкой в векторное хранилище.
